[pytest]
markers =
    integration: end-to-end pipeline tests that hit the BacDive API (slow; skipped by default).

# By default, skip tests marked 'integration' so the fast suite stays fast.
# Run them explicitly with:
#     pytest -m integration tests/
#     pytest tests/test_pipeline_integration.py
addopts = -m "not integration"
