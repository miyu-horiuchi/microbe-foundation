# Core pipeline (fetch / parse / splits / vocab / model) — works on Python 3.9+
pandas>=2.0
pyarrow>=15
# numpy <2: GPU-box torch wheels are built against the NumPy 1.x C-ABI; NumPy 2
# breaks torch<->numpy interop ("_ARRAY_API not found"). 1.26.x satisfies pandas.
numpy>=1.26,<2
tqdm>=4.66

# Genome featurization (extract_genome_accessions + microbe_model package)
pyrodigal>=3.5
biopython>=1.83
requests>=2.32
python-dotenv>=1.0

# Multi-task model (model.py)
torch>=2.2

# ESM-2 feature extraction (compute_esm2_features.py)
# NOTE: this path requires Python >= 3.11 because of transitive
# transformers/Pillow compatibility. Older Python can still run
# fetch/parse/splits/vocab/model with random features.
transformers>=4.40,<4.58  # 4.57.x verified-working. NOTE: the GPU-box "Could not
# find EsmModel" failure was NOT a version problem -- transformers' EsmModel import
# chain pulls image_utils, which needs PIL.Image.Resampling (Pillow >= 9.1). The
# box's stale apt Pillow lacked it. The LoRA path (finetune_lora.py) runs in a venv;
# see scripts/lora_box_bootstrap.sh.
Pillow>=10  # transformers image_utils needs PIL.Image.Resampling (>=9.1); pin modern

# Encoder LoRA fine-tuning (finetune_lora.py) — GPU box only. peft must be
# compatible with the transformers ceiling above; accelerate for mixed precision.
peft>=0.11,<0.14
accelerate>=0.30

# Optional baselines
scikit-learn>=1.4
scipy>=1.11
xgboost>=2.0
